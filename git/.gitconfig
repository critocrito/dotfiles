[user]
    name = crito
    email = crito@cryptodrunks.net
    signingkey = 1CFA02A2
[color]
    ui = true
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[alias]
    p  = pull
    c  = commit
    d  = diff
    s  = status
    l  = log
    a  = add
    m  = merge
    b  = branch
    r  = remote

    aa = add -A
    sh = show
    co = checkout
    nb = checkout -b
    mt = mergetool
    pu = push
    cp = cherry-pick

    addp = add --patch                 # stage commits chunk by chunk
    branch = branch -v                 # branch (verbose)
    amend = commit --amend             # amend your last commit
    cherry-pick = cherry-pick -x       # grab a change from a branch
    diffstaged   = diff --cached       # diff staged changes
    diffunstaged = diff                # complimentary to diffstage, show
                                       # unstaged changes
    last         = diff HEAD^          # diff last committed change
    log          = log --decorate --graph --date=short --graph
    stats        = log --stat          # Show log stats
    changes      = log --pretty=format:\"%h %cr %cn %Cgreen%s%Creset\" --name-status
    short        = log --pretty=format:\"%h %cr %cn %Cgreen%s%Creset\"
    changelog    = log --pretty=format:\" * %s\"
    shortnocolor = log --pretty=format:\"%h %cr %cn %s\"
    filelog      = log -u              # show changes to a file
    pull = pull --rebase               # Move rebase over merge
    remote = remote -v                 # Be verbose when printing remotes
    unstage  = reset HEAD              # remove files from index (tracking)
    uncommit = reset --soft HEAD^      # go back before last commit, with files
    g = grep --break --heading --line-number
                                       # in uncommitted state
[format]
    pretty = format:%C(blue)%ad%Creset %C(yellow)%h%C(green)%d%Creset %C(blue)%s %C(magenta) [%an]%Creset
[merge]
    summary = true
    verbosity = 1
    tool = vimdiff3
    #tool = fugitive
[apply]
    whitespace = nowarn
[branch]
    autosetupmerge = true
[push]
    # 'git push' will push the current branch to its tracking branch
    # the usual default is to push all branches
    default = tracking
[core]
    autocrlf = false
    #editor = e
    #excludesfile = ~/.dotfiles/gitignore_global
[advice]
    statusHints = false
[diff]
    # Git diff will use (i)ndex, (w)ork tree, (c)ommit and (o)bject
    # instead of a/b/c/d as prefixes for patches
    mnemonicprefix = true
[rerere]
    # Remember my merges
    # http://gitfu.wordpress.com/2008/04/20/git-rerere-rereremember-what-you-did-last-time/
    enabled = true
[github]
    user = crito
[pull]
    rebase = true
[mergetool "fugitive"]
    cmd = vim -f -c \"Gdiff\" \"$MERGED\"
[mergetool "vimdiff3"]
    cmd = vim -f -d -c 'wincmd J' -c 'resize 22' $MERGED $LOCAL $BASE $REMOTE
[grep]
	extendRegexp = true
	lineNumber = true
[sendemail]
	smtpuser = crito
	smtpserver = mx20.cryptodrunks.net
	smtpencryption = tls
	smtpserverport = 587
	suppresscc = self
[pager]
	log = diff-highlight | less
	show = diff-highlight | less
	diff = diff-highlight | less
