_deps() {
  if is_not_mac;
  then
    ensure_pkg "fzf"
    ensure_pkg "fasd"
  fi

  if is_mac;
  then
    brew_bundle "shell-goodies/Brewfile"
  fi
}

_install() {
  if is_mac;
  then
    /opt/homebrew/opt/fzf/install  --key-bindings --completion --no-update-rc
  fi
}
